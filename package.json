{
  "name": "bitebuddy",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "api:contract:generate": "node scripts/generate-openapi.cjs",
    "api:contract:check": "node scripts/generate-openapi.cjs --check",
    "test": "npm -w @bitebuddy/shared run test && npm -w @bitebuddy/api run test"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "ajv": "^8.18.0"
  }
}
